import styled from "styled-components";

const ContentContainer = styled.div`
  width: clamp(60vw, 90rem, 95vw);
  margin: 0 auto;
  padding-block: clamp(0.1rem, 1vw, 0.5rem);
`;

export default ContentContainer;